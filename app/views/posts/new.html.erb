<div class="wrap">
  <div class="form-box">  
    <% if current_user && current_user.admin %>
      <h1>New Post</h1>
      <div class="clearfix">
        <%= render 'form', post: @post %>
      </div>
      <div class="new-form-back"><%= link_to 'Back', posts_path %></div>
    <% end %>
  </div>
</div>

  