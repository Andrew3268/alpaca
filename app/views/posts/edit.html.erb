<div class="wrap">  
  <% if current_user && current_user.admin %>
    <div class="form-left">
      <h1>Editing Post</h1>
      <%= simple_form_for(@post) do |f| %>
        <%= f.error_notification %>
        <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
        <%= f.select :p_spare_01, ([["No Background?", ""], "YES"])%>
        <%= f.select :p_spare_05, ([["로켓배송", ""], "YES"])%>

        <div class="form-inputs">
          <div class="select-box clearfix">
          <%= f.association :category %>
          <%= f.association :shop %>
          </div>
          
          <div class="input-box">
            <%= f.file_field :post_image %>
            <%= f.input :title, label: '상품명'  %>
            <%= f.input :link, label: '링크' %>
            <%= f.input :source, label: '판매자' %>
            <%= f.input :p_spare_06, label: '판매 사이트' %>
            <!-- <%= f.input :video %> -->
            <!-- <%= f.input :image_toggle %> -->
            <!-- <%= f.input :description %> -->
            <%= f.input :is_price, label: '현재가격'  %>
            <%= f.input :was_price, label: '할인전 가격' %>
            <%= f.input :p_spare_03, label: '평점' %>
            <%= f.input :p_spare_04, label: '리뷰' %>
            <%= f.input :p_spare_02, label: 'HashTag' %>
            <!-- <%= f.input :pct %> -->
          </div>
        </div>

        <div class="form-actions">
          <%= f.button :submit %>
        </div>
      </div>
    <% end %>

    <div class="form-right">
      <em>필수 HashTag'S</em>
      <p>
        <span>좋아하는 색깔은?</span><br/>
        #레드, #블랙, #화이트, #핑크, #퍼플, #블루, #그린, #옐로우, #브라운, #그레이, #메탈, #믹스...
      </p>

      <p>
        <span>얼마면되?</span><br/>
        #1만원이하, #1만원대, #2만원대... 10만원이상
      </p>

      <p>
        <span>무슨 선물을 원해?</span><br/>
        #생일선물, #집들이선물, #신혼선물, ...
      </p>

      <p>
        <span>누구를 위한 선물이야?</span><br/>
        #엄마, #아빠, #친구, #남자친구, #여자친구, ...
      </p>

      <p>
        <span>그 사람은 어떤 사람이야?</span><br/>
        
      </p>

      <p>
        <span>어떤 브랜드 좋아해?</span><br/>
        #아디다스, #나이키
      </p>
    </div>

    <%= link_to 'Back', posts_path %>
  <% end %>
</div>